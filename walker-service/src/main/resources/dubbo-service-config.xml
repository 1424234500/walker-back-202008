<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	   xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 可配置于classPath:dubbo.properties -->


	<!-- name提供方应用名 owner负责人 aa-->
	<dubbo:application name="dubbo_service_consumer" owner="walker" logger="log4j"  />

	<!-- 提供服务端口 -->
	<dubbo:protocol name="dubbo" port="20881" />

	<!-- 注册中心 -->

	<!-- zookeeper 版本可配？ enable="true" ,122.10.1.12:2182 register="false" 只订阅-->
	<dubbo:registry address="localhost:2181" protocol="zookeeper" register="false" />
	<!-- redis -->		
	
	<!-- 监控中心 -->
	<!-- 从注册中心获取监控地址 --> 
	<!-- <dubbo:monitor id="dubbo_provider_monitor" protocal="registry"  />  -->
	<!-- 直连 -->
	<!-- <dubbo:monitor address="122.21.21.12:2213" /> -->	

	<!-- 指定了集群容错模式，此处为快速失败 -->
    <dubbo:provider cluster="failfast"/>

	<!-- 1.提供者 服务提供 ref：指向注解定义的本地bean（多实现分组） interface：指向接口-->
<!--	<dubbo:service ref="echoService" interface="com.walker.service.EchoService" timeout="2048" version="1.0"  />-->

	<!-- 2.消费者 消费服务 id:bean名字 interface:指向远程接口 group:多实现分组区分 -->
	<dubbo:reference id="echoService" interface="com.walker.service.EchoService" version="1.0" timeout="1024"></dubbo:reference>


</beans>
